# Core scripts use only the Python standard library.
# These extras are needed for downstream analysis (single-cell, plotting, etc.).
pandas>=2.0
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
pyarrow>=14
anndata>=0.10
scanpy>=1.9
scikit-learn>=1.3
umap-learn>=0.5
