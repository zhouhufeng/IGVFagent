{
  "schema_version": "1.0",
  "id": "mpra",
  "version": "0.2.9",
  "title": "MPRA / STARR / BlueSTARR retrieval and analysis",
  "summary": "",
  "module": "igvfagent.mpra_data_skills",
  "source": "Scripts/mpra_data_skills.py",
  "interfaces": {
    "cli": "igvfagent mpra",
    "tools": [
      {
        "name": "mpra_activity",
        "subcommand": "mpra activity",
        "description": "Per-oligo MPRA activity: negative-binomial GLM Wald test (DESeq2 via pydeseq2) of RNA vs DNA counts, followed by summit-shift normalization that moves the mode of the log2-fold-change density to zero (Tewhey-lab MPRAmodel convention). Emits a .out table with baseMean, log2FoldChange, lfcSE, stat, pvalue, padj per Oligo. Input: a counts table with an 'Oligo' column and DNA_*/RNA_* replicate columns; barcode-level tables (with a 'Barcode' column) are summed per oligo automatically.",
        "parameters": {
          "type": "object",
          "properties": {
            "input": {
              "type": "string",
              "description": "Path to counts table (CSV/TSV)."
            },
            "label": {
              "type": "string"
            }
          },
          "required": [
            "input"
          ]
        }
      },
      {
        "name": "mpra_pull",
        "subcommand": "mpra pull",
        "description": "Pull MPRA/STARR/BlueSTARR metadata from Catalog or Portal. Backed by /api/variants/biosamples, where the reporter assay is carried by the Catalog 'method' field: 'STARR-seq' for the measured assay and 'BlueSTARR' for the neural-network predictions over it (source='IGVF'). Those are distinct record sets \u2014 say which one the user means rather than conflating them. This collection is region-queryable (e.g. chr4:155600-155770).",
        "parameters": {
          "type": "object",
          "properties": {
            "source": {
              "type": "string",
              "enum": [
                "catalog",
                "portal"
              ],
              "default": "catalog"
            },
            "limit": {
              "type": "integer",
              "default": 25
            }
          }
        }
      },
      {
        "name": "mpra_qc",
        "subcommand": "mpra qc",
        "description": "MPRA QC suite: replicate concordance (pairwise Pearson r on log10 counts, separately for DNA and RNA, with an N x N SVG heatmap), unique-barcodes-per-oligo histogram (when a 'Barcode' column is present), and total-counts-per-oligo histogram (log10). Writes a markdown QC report alongside the SVGs.",
        "parameters": {
          "type": "object",
          "properties": {
            "input": {
              "type": "string",
              "description": "Counts table (barcode-level supported)."
            },
            "label": {
              "type": "string"
            }
          },
          "required": [
            "input"
          ]
        }
      },
      {
        "name": "mpra_skew",
        "subcommand": "mpra skew",
        "description": "Allelic-skew analysis: pairs ref/alt oligos by SNP_window_strand_haplotype, computes per-replicate log2((RNA+1)/(mean DNA+1)) for each allele, runs a paired t-test of alt vs ref across replicates, and applies BH-FDR. Emits a .out table with Log2Skew, LogSkew_SE, tstat, pvalue, padj per element. Mirrors the t-test path of Tewhey-lab MPRAmodel runSkew.",
        "parameters": {
          "type": "object",
          "properties": {
            "input": {
              "type": "string",
              "description": "Counts table with Allele plus at least one of SNP/Window/Strand/Haplotype."
            },
            "label": {
              "type": "string"
            }
          },
          "required": [
            "input"
          ]
        }
      },
      {
        "name": "mpra_volcano",
        "subcommand": "mpra volcano",
        "description": "Render a 4-panel volcano figure (activity volcano, allelic-skew volcano, activity MA plot, skew MA plot) from the .out tables produced by `mpra activity` and `mpra skew`. Points with padj below the FDR threshold are highlighted in red.",
        "parameters": {
          "type": "object",
          "properties": {
            "activity": {
              "type": "string",
              "description": "Activity .out table from `mpra activity`."
            },
            "skew": {
              "type": "string",
              "description": "Skew .out table from `mpra skew`."
            },
            "label": {
              "type": "string"
            },
            "title": {
              "type": "string"
            },
            "fdr": {
              "type": "number",
              "default": 0.05
            }
          }
        }
      }
    ]
  },
  "upstream_resources": [
    "catalog_api",
    "encode",
    "portal_api"
  ],
  "artefact_kinds": [
    "Manifest"
  ],
  "validation": {
    "status": "benchmarked",
    "method": "machine-checked reproduction of published results",
    "evidence": [
      {
        "benchmark": "agarwal2025_lentimpra",
        "paper": {
          "authors": "Agarwal V et al.",
          "journal": "Nature",
          "year": 2025,
          "doi": "10.1038/s41586-024-08430-9",
          "pmid": "39814879"
        },
        "checks": 3,
        "run": "Benchmarks/agarwal2025_lentimpra/run.sh"
      },
      {
        "benchmark": "deng2024_cortex_mpra",
        "paper": {
          "authors": "Deng C, Whalen S et al.",
          "journal": "Science",
          "year": 2024,
          "doi": "10.1126/science.adh0559",
          "pmid": "38781390",
          "pmc": "PMC12085231"
        },
        "checks": 1,
        "run": "Benchmarks/deng2024_cortex_mpra/run.sh"
      }
    ],
    "n_checks": 4,
    "scope_note": "Benchmarks execute fixed command sequences with no model in the loop, so this establishes implementation correctness only \u2014 not that an agent would select this skill unaided. See Docs/EVALUATION.md."
  },
  "documentation": "Docs/Skills/MPRA_ANALYSIS_SKILLS.md",
  "license": "Apache-2.0"
}
