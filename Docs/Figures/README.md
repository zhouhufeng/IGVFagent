# Figures

Figures referenced by the project README and per-skill docs.

| File | Where it's used |
|---|---|
| `igvfagent_architecture.png` | Top of root `README.md` — Plan → Action → Results → Evaluation orchestrator overview |

When updating a figure, keep the filename stable so the README link does not
break. PNG and SVG copies are both welcome (commit the PNG; SVG is optional
for editable source).
